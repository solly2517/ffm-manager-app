# Trends, Email Draft, and Department Export Verification

The repaired Travel Expenses route renders correctly after the hook-order fix. Finance users see inclusive `From` and `To` date inputs, an **All departments** dropdown, and Excel/CSV download actions within the accounting export card.

The Administrator dashboard renders the six-month expense trend area beneath the monthly department-and-currency summary. With no claim data available, it presents a concise empty state rather than a misleading chart. The selected-Delegate email composition control is available only after an overdue Delegate is selected, so it was not visible in the current all-clear dashboard state.
