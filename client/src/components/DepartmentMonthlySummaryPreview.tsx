import React, { useState } from "react";
import { Copy } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { useOptionalLanguage } from "@/contexts/LanguageContext";
import { ALTAMAM_REPORT_LOGO_URL, departmentComparisonRows, departmentExecutiveSummary, type DepartmentMonthlySummary } from "@/lib/departmentMonthlySummaryPdf";

type Props = { open: boolean; onOpenChange: (open: boolean) => void; summary: DepartmentMonthlySummary | null; commentary: string; onCommentaryChange: (value: string) => void; onDownload: (summary: DepartmentMonthlySummary) => void; onShare: (commentary: string) => void; isSharing: boolean; shareLink: string };

export function DepartmentMonthlySummaryPreview({ open, onOpenChange, summary, commentary, onCommentaryChange, onDownload, onShare, isSharing, shareLink }: Props) {
  const { language } = useOptionalLanguage();
  const [copied, setCopied] = useState(false);
  const c = language === "ar" ? { fallback: "تقرير شهري", title: "ملخص الأقسام الشهري", description: "راجع بيانات التقرير المصرح بها قبل إنشاء ملف PDF أو مشاركته بأمان.", to: "إلى", departments: "أقسام", caveat: "تعكس أعداد أدوار الأعضاء التعيينات الحالية. تستخدم أعداد المهام وسجل العمل الشهر المحدد.", executive: "نظرة تنفيذية", members: "الأعضاء الحاليون", tasks: "مهام الشهر", workLog: "نشاط سجل العمل", topTasks: "أعلى حجم للمهام", topWorkLog: "أعلى نشاط لسجل العمل", noData: "لا توجد بيانات للأقسام", commentary: "تعليق اختياري لمسؤول النظام", commentaryPlaceholder: "أضف السياق أو القرارات أو الخطوات التالية لقراء التقرير.", secureLink: "رابط مشاركة آمن لمسؤول النظام", secureDescription: "ينتهي هذا الرابط خلال 30 يومًا ويتطلب مسؤول نظام مسجلاً دخوله؛ لا توضع بيانات التقرير في الرابط.", copied: "تم النسخ", copy: "نسخ", back: "العودة إلى لوحة التحكم", creating: "جارٍ إنشاء الرابط الآمن…", share: "مشاركة التقرير", download: "تنزيل PDF" } : { fallback: "Monthly report", title: "Monthly Department Summary", description: "Review the authorized report data before generating or securely sharing the PDF.", to: "to", departments: "departments", caveat: "Member-role counts reflect current department assignments. Task and Work Log counts use the selected month.", executive: "Executive overview", members: "Current members", tasks: "Monthly tasks", workLog: "Work Log activity", topTasks: "Top task volume", topWorkLog: "Top Work Log activity", noData: "No department data", commentary: "Optional Administrator commentary", commentaryPlaceholder: "Add context, decisions, or next steps for report readers.", secureLink: "Secure Administrator share link", secureDescription: "This link expires in 30 days and requires an authenticated Administrator; report data is not placed in the URL.", copied: "Copied", copy: "Copy", back: "Back to dashboard", creating: "Creating secure link…", share: "Share Report", download: "Download PDF" };
  const comparison = departmentComparisonRows(summary?.totals ?? []);
  const executive = departmentExecutiveSummary(summary?.totals ?? []);
  const maximum = Math.max(1, ...comparison.flatMap(item => [item.members, item.tasks, item.workLog]));
  const monthLabel = summary ? new Intl.DateTimeFormat(language === "ar" ? "ar-EG-u-ca-gregory" : "en-GB", { month: "long", year: "numeric", timeZone: "UTC" }).format(new Date(`${summary.month}-01T00:00:00.000Z`)) : c.fallback;
  const copyShareLink = async () => { if (!shareLink) return; await navigator.clipboard?.writeText(shareLink); setCopied(true); };
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90vh] max-w-4xl overflow-y-auto border-teal-900/30 bg-[#f7faf8] text-[#19373b]"><DialogHeader><div className="flex flex-wrap items-start justify-between gap-4 pr-8"><div className="rounded-xl border border-teal-900/10 bg-white px-3 py-2 shadow-sm"><img src={ALTAMAM_REPORT_LOGO_URL} alt="Al Tamam Medical Corporation" className="h-10 w-auto" /></div><div className="text-right"><DialogTitle className="text-[#19373b]">{c.title}</DialogTitle><DialogDescription className="text-[#5b6c6c]">{c.description}</DialogDescription></div></div></DialogHeader>{summary && <div className="space-y-5"><div className="rounded-xl border border-teal-900/10 bg-white p-4"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-lg font-semibold text-[#19373b]">{monthLabel}</p><p className="text-sm text-[#5b6c6c]">{summary.from} {c.to} {summary.to}</p></div><Badge className="border border-teal-900/15 bg-[#dff1ed] text-[#1b6f6f]">{summary.totals.length} {c.departments}</Badge></div><p className="mt-3 text-xs text-[#5b6c6c]">{c.caveat}</p></div><div className="rounded-xl border border-teal-900/10 bg-white p-4"><p className="mb-3 text-sm font-semibold">{c.executive}</p><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-lg bg-[#f6faf8] p-3"><span className="text-xs text-[#5b6c6c]">{c.members}</span><strong className="block text-2xl text-[#1b6f6f]">{executive.memberCount}</strong></div><div className="rounded-lg bg-[#f6faf8] p-3"><span className="text-xs text-[#5b6c6c]">{c.tasks}</span><strong className="block text-2xl text-[#1b6f6f]">{executive.taskCount}</strong></div><div className="rounded-lg bg-[#f6faf8] p-3"><span className="text-xs text-[#5b6c6c]">{c.workLog}</span><strong className="block text-2xl text-[#1b6f6f]">{executive.workLogCount}</strong></div></div><p className="mt-3 text-sm text-[#5b6c6c]">{c.topTasks}: <strong className="text-[#19373b]">{executive.topTaskDepartment ? `${executive.topTaskDepartment.name} (${executive.topTaskDepartment.taskCount})` : c.noData}</strong> · {c.topWorkLog}: <strong className="text-[#19373b]">{executive.topWorkLogDepartment ? `${executive.topWorkLogDepartment.name} (${executive.topWorkLogDepartment.weeklyPlanCount + executive.topWorkLogDepartment.dailyReportCount})` : c.noData}</strong></p></div><div className="rounded-xl border border-teal-900/10 bg-white p-4"><div className="mb-3 flex flex-wrap gap-3 text-xs font-medium"><span className="text-[#1b6f6f]">■ {c.members}</span><span className="text-[#55cfcc]">■ {c.tasks}</span><span className="text-[#19373b]">■ {c.workLog}</span></div><div className="space-y-4">{comparison.map(row => <div key={row.name}><div className="mb-1 flex justify-between gap-3 text-sm"><strong>{row.name}</strong><span className="text-[#5b6c6c]">{row.members} {c.members} · {row.tasks} {c.tasks} · {row.workLog} {c.workLog}</span></div><div className="space-y-1"><div className="h-2 rounded-full bg-[#e7efed]"><div className="h-2 rounded-full bg-[#1b6f6f]" style={{ width: `${(row.members / maximum) * 100}%` }} /></div><div className="h-2 rounded-full bg-[#e7efed]"><div className="h-2 rounded-full bg-[#55cfcc]" style={{ width: `${(row.tasks / maximum) * 100}%` }} /></div><div className="h-2 rounded-full bg-[#e7efed]"><div className="h-2 rounded-full bg-[#19373b]" style={{ width: `${(row.workLog / maximum) * 100}%` }} /></div></div></div>)}</div></div><div className="rounded-xl border border-teal-900/10 bg-white p-4"><Label htmlFor="monthly-report-commentary" className="text-[#19373b]">{c.commentary}</Label><Textarea id="monthly-report-commentary" value={commentary} onChange={event => { onCommentaryChange(event.target.value.slice(0, 2000)); setCopied(false); }} maxLength={2000} placeholder={c.commentaryPlaceholder} className="mt-2 min-h-28 border-teal-900/15 bg-[#fdfefd] text-[#19373b]"/><p className="mt-2 text-right text-xs text-[#5b6c6c]">{commentary.length}/2,000</p></div>{shareLink && <div className="rounded-xl border border-teal-900/10 bg-[#dff1ed] p-4"><p className="text-sm font-semibold text-[#19373b]">{c.secureLink}</p><p className="mt-1 text-xs text-[#5b6c6c]">{c.secureDescription}</p><div className="mt-3 flex gap-2"><Input aria-label={c.secureLink} readOnly value={shareLink} className="bg-white text-[#19373b]"/><Button type="button" variant="outline" onClick={copyShareLink}><Copy size={16}/>{copied ? c.copied : c.copy}</Button></div></div>}</div>}<DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>{c.back}</Button><Button variant="outline" disabled={!summary || isSharing} onClick={() => onShare(commentary)}>{isSharing ? c.creating : c.share}</Button><Button className="bg-[#1b6f6f] text-white hover:bg-[#155859]" disabled={!summary} onClick={() => summary && onDownload({ ...summary, commentary: commentary.trim() || null })}>{c.download}</Button></DialogFooter></DialogContent></Dialog>;
}
