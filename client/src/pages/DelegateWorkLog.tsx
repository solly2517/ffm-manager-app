import { useEffect, useState } from "react";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
import { CalendarDays, ClipboardCheck, FileText, ArrowLeft } from "lucide-react";
import { mondayForDate } from "@/lib/workLogValidation";
import { hasAtLeastHospitals, scheduleAsText, scheduleIsComplete, sixDaySchedule, type ScheduledClinicalVisit } from "@/lib/workLogSchedule";

const localDate = () => new Date().toISOString().slice(0, 10);
const statusTone = (status: string) => status === "approved" || status === "reviewed" ? "badge-success" : "badge-warning";

function ClinicalRows({ entries, onChange, clients, doctors, label }: { entries: ScheduledClinicalVisit[]; onChange: (next: ScheduledClinicalVisit[]) => void; clients: Array<{id:number;name:string}>; doctors: Array<{id:number;clientId:number;name:string;specialty?:string|null}>; label: string }) {
  const update = (index: number, patch: Partial<ScheduledClinicalVisit>) => onChange(entries.map((entry, position) => position === index ? { ...entry, ...patch } : entry));
  return <div className="space-y-3">{entries.map((entry, index) => { const matching = doctors.filter((doctor) => doctor.clientId === Number(entry.clientId)); return <div className="border border-slate-700 p-3 space-y-2" key={`${label}-${index}`}><strong className="text-cyan-200">{label} {index + 1}</strong><Input type="date" value={entry.date} onChange={(event) => update(index, { date: event.target.value })}/><select className="blueprint-input" value={entry.clientId} onChange={(event) => update(index, { clientId: event.target.value, doctorId: "" })}><option value="">Select hospital</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.name}</option>)}</select><select className="blueprint-input" value={entry.doctorId} disabled={!entry.clientId} onChange={(event) => update(index, { doctorId: event.target.value })}><option value="">{entry.clientId ? "Select doctor" : "Select hospital first"}</option>{matching.map((doctor) => <option key={doctor.id} value={doctor.id}>{doctor.name}{doctor.specialty ? ` — ${doctor.specialty}` : ""}</option>)}</select></div>; })}</div>;
}

export default function DelegateWorkLog() {
  const { user, loading, isAuthenticated } = useAuth(); const utils = trpc.useUtils();
  const clientsQuery = trpc.operations.clients.useQuery(undefined, { enabled: isAuthenticated }); const doctorsQuery = trpc.operations.doctors.useQuery(undefined, { enabled: isAuthenticated });
  const weeklyPlans = trpc.delegatePlanning.weeklyPlans.useQuery(undefined, { enabled: isAuthenticated }); const dailyReports = trpc.delegatePlanning.dailyReports.useQuery(undefined, { enabled: isAuthenticated });
  const submitWeekly = trpc.delegatePlanning.submitWeeklyPlan.useMutation({ onSuccess: () => utils.delegatePlanning.weeklyPlans.invalidate() }); const submitDaily = trpc.delegatePlanning.submitDailyReport.useMutation({ onSuccess: () => utils.delegatePlanning.dailyReports.invalidate() }); const reviewWeekly = trpc.delegatePlanning.reviewWeeklyPlan.useMutation({ onSuccess: () => utils.delegatePlanning.weeklyPlans.invalidate() }); const reviewDaily = trpc.delegatePlanning.reviewDailyReport.useMutation({ onSuccess: () => utils.delegatePlanning.dailyReports.invalidate() });
  const [weekOf, setWeekOf] = useState(() => mondayForDate(localDate())); const [weekEntries, setWeekEntries] = useState<ScheduledClinicalVisit[]>(() => sixDaySchedule(mondayForDate(localDate()))); const [dailyEntries, setDailyEntries] = useState<ScheduledClinicalVisit[]>(() => sixDaySchedule(localDate()).slice(0, 3)); const [summary, setSummary] = useState(""); const [outcomes, setOutcomes] = useState(""); const [challenges, setChallenges] = useState(""); const [nextActions, setNextActions] = useState(""); const [notice, setNotice] = useState(""); const [noticeError, setNoticeError] = useState(false);
  const isDelegate = user?.role === "delegate"; const isManager = user?.role === "manager"; const clients = clientsQuery.data ?? []; const doctors = doctorsQuery.data ?? []; const busy = clientsQuery.isLoading || doctorsQuery.isLoading; const clientName = (id: string | number) => clients.find((client) => client.id === Number(id))?.name || "Hospital"; const doctorName = (id: string | number) => doctors.find((doctor) => doctor.id === Number(id))?.name || "Doctor";
  useEffect(() => { setWeekEntries(sixDaySchedule(weekOf)); }, [weekOf]);
  const validate = (entries: ScheduledClinicalVisit[], kind: string) => { if (!scheduleIsComplete(entries)) { setNoticeError(true); setNotice(`Complete date, hospital, and doctor for all ${entries.length} ${kind} entries.`); return false; } if (!hasAtLeastHospitals(entries, 3)) { setNoticeError(true); setNotice(`Select at least three different hospitals in the ${kind} entries.`); return false; } return true; };
  const submitPlan = () => { if (!validate(weekEntries, "weekly plan")) return; const first = weekEntries[0]; submitWeekly.mutate({ clientId:Number(first.clientId), doctorId:Number(first.doctorId), weekOf:new Date(`${weekOf}T12:00:00`), objectives:"Six-day multi-hospital Delegate visit plan", plannedVisits:scheduleAsText(weekEntries, clientName, doctorName) }, { onSuccess:()=>{setNoticeError(false);setNotice("Six-day multi-hospital plan submitted for Manager review.");}, onError:(error)=>{setNoticeError(true);setNotice(error.message);} }); };
  const submitReport = () => { if (!validate(dailyEntries, "daily report")) return; if (summary.trim().length < 3 || outcomes.trim().length < 3) { setNoticeError(true);setNotice("Enter an activity summary and outcome before submitting the daily report.");return; } const first=dailyEntries[0]; const clinicalVisits=scheduleAsText(dailyEntries, clientName, doctorName); submitDaily.mutate({ clientId:Number(first.clientId), doctorId:Number(first.doctorId), reportDate:new Date(`${dailyEntries[0].date}T12:00:00`), summary:`${clinicalVisits}\n\n${summary}`, outcomes, challenges:challenges||undefined, nextActions:nextActions||undefined }, { onSuccess:()=>{setSummary("");setOutcomes("");setChallenges("");setNextActions("");setNoticeError(false);setNotice("Multi-hospital daily report submitted for Manager review.");},onError:(error)=>{setNoticeError(true);setNotice(error.message);} }); };
  if (loading) return <div className="blueprint-page"><div className="blueprint-loader">Loading FFM Work Log…</div></div>; if (!isDelegate && !isManager) return <div className="blueprint-page"><Card className="login-card blueprint-card"><h1>Work Log is role restricted</h1><p className="muted">This workspace is for Delegates and their assigned Managers.</p></Card></div>;
  return <div className="blueprint-page"><div className="blueprint-grid"/><main className="container py-8 relative z-10"><a href={isDelegate?"/delegate":"/"} className="inline-flex items-center gap-2 text-sm text-cyan-300 mb-6"><ArrowLeft size={16}/> Back to workspace</a><div className="mb-8"><p className="eyebrow">FFM / FIELD PERFORMANCE</p><h1 className="text-4xl font-bold text-white">{isDelegate?"My multi-hospital plans & reports":"Delegate plans & reports"}</h1><p className="muted mt-2">Delegates select at least three hospitals and any linked doctors directly from FFM.</p></div>{notice&&<div className={`admin-feedback mb-5 ${noticeError?"error":"success"}`}>{notice}</div>}{isDelegate&&<div className="grid lg:grid-cols-2 gap-6"><Card className="blueprint-card"><CardHeader><CardTitle className="flex items-center gap-2"><CalendarDays size={20}/> Six-day weekly plan</CardTitle><p className="muted">Six dates, at least three hospitals, and any number of doctors linked to each hospital.</p></CardHeader><CardContent className="form-stack"><label>Week starting Monday</label><Input type="date" value={weekOf} onChange={(event)=>setWeekOf(mondayForDate(event.target.value))}/><ClinicalRows entries={weekEntries} onChange={setWeekEntries} clients={clients} doctors={doctors} label="Day"/><Button className="blueprint-button" disabled={busy||submitWeekly.isPending} onClick={submitPlan}>{submitWeekly.isPending?"Submitting…":"Submit six-day plan"}</Button></CardContent></Card><Card className="blueprint-card"><CardHeader><CardTitle className="flex items-center gap-2"><FileText size={20}/> Daily visits & report</CardTitle><p className="muted">Record at least three hospital visits and all selected doctors in one daily report.</p></CardHeader><CardContent className="form-stack"><ClinicalRows entries={dailyEntries} onChange={setDailyEntries} clients={clients} doctors={doctors} label="Hospital visit"/><label>Activity summary</label><Textarea value={summary} onChange={(event)=>setSummary(event.target.value)} placeholder="Visit and key activity…"/><label>Outcomes achieved</label><Textarea value={outcomes} onChange={(event)=>setOutcomes(event.target.value)} placeholder="Decisions, completed actions, and results…"/><label>Challenges (optional)</label><Textarea value={challenges} onChange={(event)=>setChallenges(event.target.value)} placeholder="Delays or help needed…"/><label>Next actions (optional)</label><Textarea value={nextActions} onChange={(event)=>setNextActions(event.target.value)} placeholder="Action — owner — expected date…"/><Button className="blueprint-button" disabled={busy||submitDaily.isPending} onClick={submitReport}>{submitDaily.isPending?"Submitting…":"Submit daily report"}</Button></CardContent></Card></div>}<div className="grid lg:grid-cols-2 gap-6 mt-6"><Card className="blueprint-card"><CardHeader><CardTitle>Weekly plans</CardTitle></CardHeader><CardContent className="space-y-4">{weeklyPlans.isLoading?<p className="muted">Loading weekly plans…</p>:weeklyPlans.data?.length?weeklyPlans.data.map((plan)=><div key={plan.id} className="border border-slate-700 p-4"><div className="flex justify-between gap-3"><strong>{isManager?plan.delegateName:`Week of ${new Date(plan.weekOf).toLocaleDateString()}`}</strong><Badge className={statusTone(plan.status)}>{plan.status}</Badge></div><p className="mt-2 muted whitespace-pre-wrap">{plan.plannedVisits}</p>{isManager&&plan.status==="pending"&&<div className="flex gap-2 mt-3"><Button size="sm" className="blueprint-button" onClick={()=>reviewWeekly.mutate({id:plan.id,status:"approved"})}>Approve</Button><Button size="sm" variant="outline" onClick={()=>reviewWeekly.mutate({id:plan.id,status:"rejected",reviewNote:"Please revise the plan and resubmit."})}>Request revision</Button></div>}</div>):<p className="muted">No weekly plans submitted yet.</p>}</CardContent></Card><Card className="blueprint-card"><CardHeader><CardTitle>Daily reports</CardTitle></CardHeader><CardContent className="space-y-4">{dailyReports.isLoading?<p className="muted">Loading daily reports…</p>:dailyReports.data?.length?dailyReports.data.map((report)=><div key={report.id} className="border border-slate-700 p-4"><div className="flex justify-between gap-3"><strong>{isManager?report.delegateName:new Date(report.reportDate).toLocaleDateString()}</strong><Badge className={statusTone(report.status)}>{report.status}</Badge></div><p className="mt-2 text-white whitespace-pre-wrap"><strong>Visits & summary:</strong>{`\n${report.summary}`}</p><p className="mt-2 muted"><strong>Outcomes:</strong> {report.outcomes}</p>{isManager&&report.status==="submitted"&&<Button size="sm" className="blueprint-button mt-3" onClick={()=>reviewDaily.mutate({id:report.id})}><ClipboardCheck size={15}/> Mark reviewed</Button>}</div>):<p className="muted">No daily reports submitted yet.</p>}</CardContent></Card></div></main></div>;
}
